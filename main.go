package main

import (
	"github.com/Feride3d/payment-service-emulator/cmd"
)

func main() {
	cmd.Execute()
	/* 	r := router.New(storage.Storage)
	   	http.ListenAndServe(":8080", r.RootHandler()) */
}
